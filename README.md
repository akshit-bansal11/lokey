# lokey

A local-only encrypted key/value vault for Windows: a `lokey` command-line tool
and a desktop app that share one vault file. No network code, ever.

Work in progress. The full guide lands with the first release. The previous
PowerShell tool this replaces is preserved at the `legacy-powershell` tag.

MIT licensed.
